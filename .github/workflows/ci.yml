name: first CI

on: 
    push:
        branches:
            - main

jobs: 
    say-hello:
        runs-on: ubuntu-latest
        steps:
            - name: hello
              run: echo "first CI run"

